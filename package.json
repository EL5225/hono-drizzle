{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:generate": "drizzle-kit generate:pg",
    "db:migrate": "bun run --hot src/libs/drizzle/migrate.ts",
    "studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.7",
    "hono": "^4.2.1",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.4",
    "drizzle-kit": "^0.20.14"
  }
}
